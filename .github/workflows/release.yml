name: Release

on:
  push:
    tags:
      - 'v*'
      - '*@*' # monorepo package tags

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v1
    with:
      publish: true
      build: pnpm run build
    permissions:
      contents: write
      id-token: write
